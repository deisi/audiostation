version: '3'

services:
  spotify:
    build:
      context: .
      args:
        - ARCH
    container_name: spotify
    image: audiostation/spotify:latest
    network_mode: "host"
    volumes:
      - /tmp/sharesound:/tmp/sharesound
