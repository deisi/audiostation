version: '3'

services:
  spotify:
    build:
      context: .
      args:
        - BUILDARCH
        - FROMIMAGE
    container_name: spotify
    image: "audiostation/spotify:${BUILDARCH}"
    network_mode: "host"
    volumes:
      - /tmp/sharesound:/tmp/sharesound
