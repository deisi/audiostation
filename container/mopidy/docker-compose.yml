version: '3'

services:
  mopidy:
    build:
      context: .
      args:
        - arch
        - fromimage
    container_name: mopidy
    image: audiostation/mopidy:${arch}
    ports:
      - "6600:6600"
      - "6680:6680"
      - "5555:5555"
